/**
 * 
 */
package skt.tmall.common.core;

/**
 * 전체 공통으로 쓰이는 상수 모음
 * 
 *  @author leegt80
 * 
 */
public interface ICommonConstants {

	/**
	 * 프로세스가 실행된후 모든 결과값은 이 값으로 매핑된다.
	 * 
	 * @author leegt80
	 */
	public static final String PROCESS_RESULT = "__result";

	/**
	 * 
	 */
	public static final String NEW_OBJECT_OID = "_new_object_oid"; // 각각 새로 생성된
	// OBJECT
	// OID

	/**
	 * 
	 */
	public static final String OBJECT_TYPE = "_object_type"; // 자신이 생성하고자 하는
	// OBJECT의 TYPE

	/**
	 * database process에서 elect * from tab 같이 where절에 바인딩 변수가 없을때 new Object[0]를
	 * 매번 리턴하는 것을 방지한다.
	 * 
	 * @author leegt80
	 */
	public static final Object[] EMPTY_PARAMETER = new Object[0];

	/**
	 * ISelectWrapper에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String PAGE_FROM = "_page_from";

	/**
	 * ISelectWrapper에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String PAGE_TO = "_page_to";

	/**
	 * ISelectWrapper에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String FETCH_SIZE = "_fetch_size";

	/**
	 * ISelectWrapper에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String IS_WRAPPING = "_is_wrapping";
	
	/**
	 * ISelectDynamicQuery에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String DYNAMIC_PARAM = "_dynamic_param";
	
	/**
	 * ISelectDynamicQuery에서 사용된다.
	 * 
	 * @author leegt80
	 */
	public static final String IS_DYNAMIC = "_is_dynamic";

	/**
	 * 사용자 검색에서 사용
	 * 
	 * @author leegt80
	 */
	public static final String PERSON_NAME = "_person_name";

	/**
	 * 전체 OID 생성 Process를 실행하고 나면 이 값으로 결과가 매핑된다.
	 * 
	 * @author leegt80
	 */
	public static final String OID_RESULT = "_oid_result";

	/**
	 * 로그인 사용자
	 * 
	 * @author leegt80
	 */
	public static final String LOGIN_PERSON = "_loging_person";
	
	/**
	 * 로그인 ID
	 * 
	 * @author leegt80
	 */
	public static final String LOGIN_ID = "_loging_id";
	
	/**
	 * 클라이언트 IP
	 * 
	 * @author leegt80
	 */
	public static final String CLIENT_IP = "_client_ip";
	
	/**
	 * 로그인 AUTHORITY
	 * put - String[]
	 * 
	 * @author leegt80
	 */
	public static final String LOGIN_AUTHORITY = "_login_authority";
	
	/**
	 * ADMIN 유무
	 * 
	 * @author leegt80
	 */
	public static final String IS_ADMIN = "_is_admin";

	/**
	 * @see com.yuraeltec.common.model.process.repository.AbstractRepositoryProcess.checkin 
	 */
	public static final String REPOSITROY_CONTEXT = "_repositroy_context";
	
	/**
	 * HttpServletRequest
	 */
	public static final String HTTP_SERVLET_REQUEST = "_http_servlet_request";
}
